(() => {
  const isVisible=(el)=>{const r=el.getBoundingClientRect(),s=getComputedStyle(el);return r.width>0&&r.height>0&&s.visibility!=='hidden'&&s.display!=='none'&&r.bottom>=0&&r.top<=innerHeight};
  const visualTokens=()=>{const out=[];for(const el of document.querySelectorAll('body *')){if(!isVisible(el)||el.children.length>0)continue;const t=(el.innerText||el.textContent||'').replace(/\s+/g,' ').trim();if(!t||t.length>120)continue;const r=el.getBoundingClientRect();out.push({text:t,x:Math.round(r.left),y:Math.round(r.top),w:Math.round(r.width),h:Math.round(r.height),cls:String(el.className||'').slice(0,160),tag:el.tagName});if(out.length>=5000)break}return out};
  const snapshot=()=>({type:'NEXT_WALKING_MEEVO_SNAPSHOT',at:new Date().toISOString(),url:location.href,title:document.title||'',visibleText:(document.body?.innerText||'').slice(0,500000),visualTokens:visualTokens()});
  chrome.runtime.onMessage.addListener((msg,_sender,sendResponse)=>{if(msg?.type==='NW_READ_VISIBLE_MEEVO')sendResponse(snapshot());return true});
})();